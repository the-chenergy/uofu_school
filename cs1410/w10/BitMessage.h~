#include <string>

using namespace std;

/**
 * This class represents a partially (or compelety) collected message.
 */
class BitMessage {
 private:
  string message;
  int tempChar;
  int bitCount;
  
 public:
  BitMessage(); // constructor
  void append(int bit);
  string getMessage();
};
